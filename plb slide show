<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<title>Slideshow Pusat Layanan Bahasa UNTAG Surabaya</title>
<style>
  body {
    margin: 0;
    padding: 0;
    background: #fafafa;
    font-family: Arial, sans-serif;
  }

  .slideshow-container {
    position: relative;
    max-width: 900px;
    margin: 30px auto;
    border-radius: 14px;
    overflow: hidden;
    box-shadow: 0 6px 20px rgba(0,0,0,0.15);
  }

  .slide {
    display: none;
    width: 100%;
    height: 500px;
  }

  .slide img {
    width: 100%;
    height: 500px;
    object-fit: cover;
  }

  .fade {
    animation-name: fadeEffect;
    animation-duration: 1.5s;
  }

  @keyframes fadeEffect {
    from { opacity: 0.3; }
    to { opacity: 1; }
  }

  .title-overlay {
    position: absolute;
    bottom: 20px;
    left: 20px;
    padding: 12px 22px;
    background: rgba(187,0,0,0.80);
    color: white;
    font-size: 22px;
    border-radius: 12px;
    font-weight: bold;
    backdrop-filter: blur(2px);
  }
</style>
</head>
<body>

<div class="slideshow-container">

  <!-- SLIDE 1 -->
  <div class="slide fade">
    <img src="https://drive.google.com/uc?export=view&id=1B6kV22cG5Y-oVqHx4RWMb_5vKGjRN3qV">
    <div class="title-overlay">Pusat Layanan Bahasa UNTAG Surabaya</div>
  </div>

  <!-- SLIDE 2 -->
  <div class="slide fade">
    <img src="https://drive.google.com/uc?export=view&id=1-mYErnbnHvHtz0vAU54pvbx4Xjy8G7t4">
    <div class="title-overlay">Layanan Konsultasi Bahasa Asing</div>
  </div>

  <!-- SLIDE 3 -->
  <div class="slide fade">
    <img src="https://drive.google.com/uc?export=view&id=1lZLO4m9L79llbP5WUVb4Fb46p9bWn5p8">
    <div class="title-overlay">Pelatihan TOEFL & Academic Writing</div>
  </div>

</div>

<script>
let slideIndex = 0;
showSlides();

function showSlides() {
  let slides = document.getElementsByClassName("slide");

  for (let i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";
  }

  slideIndex++;
  if (slideIndex > slides.length) { slideIndex = 1 }

  slides[slideIndex - 1].style.display = "block";

  setTimeout(showSlides, 3500);
}
</script>

</body>
</html>
